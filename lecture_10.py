# coumpound data types
# list

my_list = [4, 6, 7, 8, 10, 12, 14, 15, 35]
my_str_list = ["comp sci", "physics", "elec eng", "philosophy"]

'''1. sort the values in ascending order, descending order
2. find values in the list, or details about the list
3. insert or remove values from the list
4. grab sub-lists from the list to work with
5. iterate through and performs functions checks on each list item'''

# sort -> sort(), sorted()
# find -> len(), min(), max(), in, indexing, slicing, count()
# insert/remove -> append(), insert(), extend(), remove(), pop()
# sub-lists -> slicing, in-place, copying
# iteration - for loops, while loops

